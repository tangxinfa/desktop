# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $auto-lock ~/bin/desktop-lock\ i3-auto-lock
set $manual-lock ~/bin/desktop-lock

ipc-socket /tmp/i3-ipc.sock

set_from_resource $dual-screen-primary DualScreen.Primary HDMI-0
set_from_resource $dual-screen-secondary DualScreen.Secondary HDMI-1

# define colors
set $background-color 	     #ffffff
set $border-color 	         #2f343f
set $inactive-border-color   #2f343f
set $text-color              #f3f4f5
set $inactive-text-color     #676E7D
set $urgent-border-color     #E53935
set $separator-color         #757575
set $indicator-color         #00ff00
set $highlight-color         #e60053

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Source Code Pro 12,FontAwesome 12

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod
floating_minimum_size 200 x -1
floating_maximum_size 1280 x -1

focus_follows_mouse no

# start a terminal
#bindsym $mod+Return exec i3-sensible-terminal
#bindsym $mod+Return exec dbus-launch gnome-terminal --hide-menubar
bindsym $mod+Return exec gnome-terminal --role=gnome-terminal-popup --hide-menubar

# move to other tty
bindsym Control+Mod1+o exec --no-startup-id ~/bin/tty-ctl other || ~/bin/i3-ctl other
bindsym Control+$mod+o exec --no-startup-id ~/bin/tty-ctl other || ~/bin/i3-ctl other

# kill focused window
bindsym $mod+Shift+q kill

#Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad
#Show the first scratchpad window
bindsym $mod+minus scratchpad show

# power management
bindsym Control+$mod+l exec $manual-lock
bindsym Control+$mod+p exec --no-startup-id ~/bin/rofi-power-menu

# program launcher: rofi
##  depends: yaourt -S rofi
bindsym $mod+r exec --no-startup-id "killall rofi && true || rofi -p '' -show run"
bindsym $mod+d exec --no-startup-id "killall rofi && true || rofi -p '' -show drun"
bindsym $mod+t exec --no-startup-id "killall rofi && true || rofi -p '' -show '' -modi ':~/bin/rofi-ydcv' -no-sidebar-mode -fixed-num-lines -lines 14 -yoffset -10 -location 2 -theme-str '#element { padding: 0.1em; margin: 0;}'"
bindsym $mod+w exec --no-startup-id "killall rofi && true || rofi -p '' -show window"

# mouse left click
bindsym Control+$mod+Return exec --no-startup-id ~/bin/mouse-left-click

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+i move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (toggle split)
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

set $workspace1 "1:"
set $workspace2 "2:"
set $workspace3 "3:"
set $workspace4 "4:"
set $workspace5 "5:"
set $workspace6 "6:"
set $workspace7 "7:"
set $workspace8 "8:"

workspace $workspace1 output $dual-screen-primary
workspace $workspace2 output $dual-screen-primary
workspace $workspace3 output $dual-screen-primary
workspace $workspace4 output $dual-screen-primary
workspace $workspace5 output $dual-screen-primary
workspace $workspace6 output $dual-screen-primary
workspace $workspace7 output $dual-screen-primary
workspace $workspace8 output $dual-screen-secondary

# switch to workspace
bindsym $mod+1 workspace $workspace1
bindsym $mod+2 workspace $workspace2
bindsym $mod+3 workspace $workspace3
bindsym $mod+4 workspace $workspace4
bindsym $mod+5 workspace $workspace5
bindsym $mod+6 workspace $workspace6
bindsym $mod+7 workspace $workspace7
bindsym $mod+8 workspace $workspace8

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $workspace1
bindsym $mod+Shift+2 move container to workspace $workspace2
bindsym $mod+Shift+3 move container to workspace $workspace3
bindsym $mod+Shift+4 move container to workspace $workspace4
bindsym $mod+Shift+5 move container to workspace $workspace5
bindsym $mod+Shift+6 move container to workspace $workspace6
bindsym $mod+Shift+7 move container to workspace $workspace7
bindsym $mod+Shift+8 move container to workspace $workspace8

# cycle through active workspaces
bindsym $mod+Tab workspace next

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
# open system monitor
bindsym $mod+m exec gnome-system-monitor

# resize window (you can also use the mouse for that)
set $resize 　 [f]  [b]  [n]  [p]
mode "$resize" {
        bindsym f resize grow width 10 px or 10 ppt
        bindsym b resize shrink width 10 px or 10 ppt
        bindsym n resize grow height 10 px or 10 ppt
        bindsym p resize shrink height 10 px or 10 ppt
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt

        # back to normal: Enter or Escape or q
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym q mode "default"
        bindsym Control+g mode "default"
}

bindsym $mod+Shift+s mode "$resize"

# Screen brightness controls
bindsym XF86MonBrightnessDown exec --no-startup-id xbacklight - 5
bindsym XF86MonBrightnessUp exec --no-startup-id xbacklight + 5
# Screen brightness mode
set $brightness_management 　 [p]  [n]
mode "$brightness_management" {
     bindsym p exec --no-startup-id xbacklight + 5
     bindsym n exec --no-startup-id xbacklight - 5

     # back to normal: Enter or Escape or q
     bindsym Return mode "default"
     bindsym Escape mode "default"
     bindsym q mode "default"
     bindsym Control+g mode "default"
}
bindsym $mod+Shift+b mode "$brightness_management"

# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec amixer set Master playback 1000+
bindsym XF86AudioLowerVolume exec amixer set Master playback 1000-
bindsym XF86AudioMute exec amixer set Master toggle

set $volume 　 [p]  [n]  [t]  [0-9]
mode "$volume" {
        bindsym 0 exec amixer set Master playback 0%
        bindsym 1 exec amixer set Master playback 10%
        bindsym 2 exec amixer set Master playback 20%
        bindsym 3 exec amixer set Master playback 30%
        bindsym 4 exec amixer set Master playback 40%
        bindsym 5 exec amixer set Master playback 50%
        bindsym 6 exec amixer set Master playback 60%
        bindsym 7 exec amixer set Master playback 70%
        bindsym 8 exec amixer set Master playback 80%
        bindsym 9 exec amixer set Master playback 90%
        bindsym p exec amixer set Master playback 1000+
        bindsym n exec amixer set Master playback 1000-
        bindsym t exec amixer set Master toggle, mode "default"

        # back to normal: Enter or Escape or q
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym q mode "default"
        bindsym Control+g mode "default"
}
bindsym $mod+Shift+v mode "$volume"

# Media player controls
bindsym XF86AudioPlay exec playerctl play
bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

popup_during_fullscreen leave_fullscreen

# workspace arrangement
assign [class="Emacs"] $workspace2
assign [class="Gnome-terminal" window_role="gnome-terminal-window.*"] $workspace1
for_window [class="Gnome-terminal" window_role="gnome-terminal-popup.*"] floating enable, move absolute position center, resize set 800 600
for_window [class="Xdialog"] floating enable
for_window [title=".*DingTalk.*" window_type="dialog"] floating disable, move container to workspace $workspace4
for_window [title=".*WeChat.*" window_type="dialog"] floating disable, move container to workspace $workspace4
assign [class="firefox" window_type="normal"] $workspace1
assign [class="Chromium"] $workspace1
assign [class="Google-chrome"] $workspace1
assign [class="Dia"] $workspace3
assign [class="Pencil"] $workspace3
assign [class="Gimp.*"] $workspace3
assign [class="XMind"] $workspace3
assign [class="libreoffice"] $workspace3
for_window [title="LibreOffice"] move container to workspace $workspace3
assign [class="Java" title="XMind"] $workspace3
for_window [class="Dia" window_role="toolbox_window"] floating enable
assign [title="Electronic WeChat"] $workspace4
assign [class="dingtalk"] $workspace4
assign [class="TelegramDesktop"] $workspace4
for_window [class="Blueman-manager"] floating enable, move absolute position center, resize set 720 480
assign [class="Wine"] $workspace4
assign [class="VirtualBox"] $workspace6
for_window [class="VirtualBoxVM"] floating enable
assign [class="netease-cloud-music"] $workspace5
for_window [class="netease-cloud-music"] floating enable, move absolute position center, resize set 720 480
assign [class="vlc"] $workspace5
assign [class="Totem"] $workspace5
assign [class="Steam"] $workspace5
assign [title="Steam"] $workspace5
assign [class="hl_linux"] $workspace5
for_window [class="keepassxc" window_type="normal"] floating enable, move scratchpad, resize set 640 360
for_window [class="Zeal" window_type="normal"] floating enable, resize set 800 600, move absolute position center, focus
for_window [class="Conky"] floating enable, resize set 400 400, move absolute position center
for_window [class="Wine" title="迅雷网络" window_type="dialog"] floating disable, move container to workspace $workspace4 # Main RTX window
assign [class="Seafile*"] $workspace1
for_window [class="Solaar"] floating enable
for_window [class="chinese-calendar"] floating enable
for_window [class="Gnome-system-monitor"] floating enable, resize set 800 600, move absolute position center

#i3-gaps
gaps inner 10
gaps outer 0
# fix graphics glitch
# smart_gaps on
smart_borders on
# border
new_window none
hide_edge_borders both
for_window [class="^.*"] border pixel 0
for_window [class="keepassxc"] border pixel 1
for_window [class="Gnome-terminal" window_role="gnome-terminal-.*"] border pixel 1

# window colors
#                       border                  background             text                     indicator
client.focused          $border-color           $background-color      $text-color              $indicator-color
client.unfocused        $inactive-border-color  $background-color      $inactive-text-color     $indicator-color
client.focused_inactive $inactive-border-color  $background-color      $inactive-text-color     $indicator-color
client.urgent           $urgent-border-color    $background-color      $text-color              $indicator-color

# autostart
exec --no-startup-id ~/bin/xkeysnail-ctl monitor
#exec --no-startup-id /usr/lib/gsd-xsettings
#exec --no-startup-id /usr/lib/notify-osd/notify-osd
exec --no-startup-id fcitx -d
exec --no-startup-id dunst
exec --no-startup-id blueman-applet
#exec --no-startup-id mate-volume-control-applet
exec --no-startup-id ~/bin/nm-applet-monitor
exec --no-startup-id udiskie -t &
exec --no-startup-id xautolock -time 10 -locker $auto-lock
exec --no-startup-id sleep 20 && dropbox start
exec --no-startup-id seafile-applet
exec --no-startup-id gtk-launch firefox
exec --no-startup-id pidof emacs || i3-msg workspace $workspace2
exec --no-startup-id pidof emacs || emacs
exec --no-startup-id ~/bin/battery-monitor
exec --no-startup-id gtk-launch geoclue-demo-agent
exec --no-startup-id gtk-launch redshift-gtk
exec --no-startup-id gtk-launch solaar
exec --no-startup-id gtk-launch variety
exec --no-startup-id gtk-launch org.keepassxc.KeePassXC
exec_always --no-startup-id polybar-msg cmd restart || polybar top
exec --no-startup-id compton -cCGb\
     -t 0\
     --config /dev/null\
     --inactive-dim=0.15\
     --detect-transient\
     --focus-exclude 'class_g = "Rofi"'\
     --detect-client-opacity\
     --detect-rounded-corners\
     --shadow-ignore-shaped\
     --use-ewmh-active-win\
     --mark-wmwin-focused\
     --detect-client-leader
exec --no-startup-id ~/bin/desktop-on-open
exec --no-startup-id ~/bin/on-at-`~/bin/at-place`
exec_always --no-startup-id setcapslock off